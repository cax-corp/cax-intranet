# CAX Intranet Configuration

## Password

PASSWORD=intranet2026

## Instructions

1. Open login.html to access the intranet
2. Enter the password above
3. To change the password, modify auth.js (line 5)

## Security Notes

- The password is temporarily stored in sessionStorage
- The session expires after 1 hour of inactivity
- All sessionStorage data is cleared when the browser closes
